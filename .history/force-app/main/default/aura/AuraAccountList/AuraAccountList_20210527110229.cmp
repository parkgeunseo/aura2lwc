<aura:component>
    <aura:attribute name="accList" type="Account[]" />>

    <aura:iteration items="{!v.accList}" var="acc">
        <p>
            AccountName : {!acc.name}
        </p>
    </aura:iteration>
</aura:component>	
